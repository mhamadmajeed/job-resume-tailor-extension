{
  "name": "job-resume-tailor-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute resume-tailor-db --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute resume-tailor-db --remote --file=schema.sql",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
